{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The three native surfaces of ADR-0033 and nothing else: the credential store, the close and the updater.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "updater:default",
    "process:allow-restart"
  ]
}
