# Known defects

Every known defect in Gobblet Online, one row each. The release gate of
[`product-spec.md` section 21.2](product-spec.md) reads this file: an open row at `critical` or
`high` fails `pnpm gates`. The format is fixed because a program parses it
([ADR-0039](adr/0039-the-defect-register-is-a-gate.md)).

Severity, and what it means here:

| Severity   | Meaning                                                                                  |
| ---------- | ---------------------------------------------------------------------------------------- |
| `critical` | Data loss, a wrong match result, an accepted illegal move, or the service being unusable |
| `high`     | A player cannot complete a normal journey, or a documented promise is broken             |
| `medium`   | A journey is completeable but degraded, or a promise holds only on some platforms        |
| `low`      | Cosmetic, or an inconvenience with an obvious workaround                                 |

Status, and what it means here:

| Status     | Meaning                                                                        |
| ---------- | ------------------------------------------------------------------------------ |
| `open`     | Present in the current build                                                   |
| `fixed`    | Fixed, with the fix named in the evidence column                               |
| `accepted` | Present, deliberately not fixed for this release, with who accepted it and why |

A row at `critical` or `high` cannot be waved through by marking it `accepted`. Accepting it means
arguing the severity down, in this file, where the argument is reviewable.

| ID     | Severity | Status   | Area          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                                                                                                                                                                                                                                         |
| ------ | -------- | -------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-0001 | medium   | open     | web client    | The main JavaScript chunk is about 1.9 MB before compression, over Vite's 500 kB warning, because Three.js is not split                                                                                                                                                                                                                                                                                                                                 | `pnpm --filter @gobblet/web run build` prints the warning; the flat tier still renders without WebGL                                                                                                                                                                                             |
| D-0002 | low      | accepted | accessibility | No screen reader pass has been performed. Accepted for the release candidate by the maintainer: the board exposes roles and labels and the keyboard journey is tested, but VoiceOver and NVDA have not been driven by a person                                                                                                                                                                                                                          | `e2e/tests/keyboard.spec.ts`, `packages/game-ui/test/scene-description.test.tsx`; `docs/compatibility.md` section 4                                                                                                                                                                              |
| D-0003 | low      | accepted | packaging     | Some GitHub actions still run on Node 20 and print a deprecation warning in every workflow. Accepted by the maintainer: it is noise in the log, and the actions are pinned to majors that will move themselves                                                                                                                                                                                                                                          | Any workflow run's annotations                                                                                                                                                                                                                                                                   |
| D-0004 | low      | accepted | database      | Six branches in `packages/db/src/backup.ts` are still uncovered: fallbacks on aggregate rows that PostgreSQL always returns, and index lookups that exist only for `noUncheckedIndexedAccess`. Accepted by the maintainer: reaching them means injecting a client into a module that deliberately opens its own, which is a change to production code for a coverage number                                                                             | `pnpm --filter @gobblet/db run test:coverage` reports 100 percent of statements, functions and lines and 98.25 percent of branches; `@gobblet/config` is at 100 percent throughout                                                                                                               |
| D-0005 | high     | fixed    | web client    | `BoardView` settled its render tier in an effect that depended on the caller's callback identity, so it re-ran on every render. The match screen re-renders with each clock tick, so the board asked the machine for a new graphics context and posted a telemetry event about ten times a second per player: browsers began discarding live contexts, the telemetry route answered 429, and the tab was busy enough to miss socket pings               | Observed in production on 2026-07-28: about twenty `POST /v1/telemetry/events` per second, all 429, and `Too many active WebGL contexts` in the browser console. Fixed by holding the callbacks in a ref; `packages/game-ui/test/board-view.test.tsx` asserts one settlement across nine renders |
| D-0006 | high     | fixed    | web client    | After a reconnection the client resent its pending command whichever board the server described, because the retry was decided from state captured before the resync was applied. A command the board had passed was sent anyway, and the rejection it earned told the player "The board had already moved on, so the move was not played" about a move that had in fact been played                                                                    | Reported in production on 2026-07-28 with ten accepted moves in the database and the message on every one. Fixed by deciding from the snapshot the resync returned; `apps/web/test/use-match-channel.test.tsx` asserts no resend once the board has moved past the command                       |
| D-0007 | medium   | fixed    | server        | A socket joined a match room and never left it, so a player who started a second match still received the first one's broadcasts. A snapshot for another match replaces the board being shown, so a resignation in an abandoned match could take over a live one                                                                                                                                                                                        | Seen in `match_connection_events` in production: one socket attached to two matches at once. Fixed by leaving other match rooms on attach; `apps/server/test/socket-gateway.test.ts` asserts a moved socket stops receiving the old match                                                        |
| D-0008 | high     | fixed    | web client    | In the WebGL tiers every pointer move was submitted twice. A square's tile and its highlight rim are two surfaces under one click handler, and a ray reports both, so the handler ran once per surface. The first command was accepted and the second carried the version from before it, which the server correctly rejected as stale: the board showed the move, and the player was told "The board had already moved on, so the move was not played" | Production counters showed 7 moves accepted and 6 stale-version rejections in one match. `e2e/tests/scene-pointer.spec.ts` counts the commands two gestures send, and read four before the fix                                                                                                   |
